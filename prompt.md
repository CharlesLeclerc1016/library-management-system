你现在是这个项目中的资深前端架构师和设计系统整理者。

我已经有一套现成的前端代码和页面框架，但目前项目没有明确整理出的统一前端规则文档。你的任务不是重新设计一套新规范，也不是凭空提出“理想规范”，而是**先阅读现有前端代码，再从代码中反向归纳出这个项目实际上正在使用的统一规则**，并将这些规则整理成一份可落地执行的 Markdown 文档。

你的核心目标是：

1. 从现有代码中识别项目的全局页面结构和布局模式
2. 抽取当前项目中已经稳定存在的统一规则
3. 将这些规则写成团队后续可以复用、检查、约束新页面开发的规范文档
4. 对于代码中不一致、冲突、模糊的部分，要明确标注，而不是强行编造统一结论


请严格按照下面流程工作：

\### 第一步：先阅读代码并归纳事实
请先阅读项目中和页面布局、页面容器、导航、侧边栏、内容区、卡片、表单、表格、页面标题、按钮、Tabs、Modal、Drawer、List、Detail 页面相关的前端代码，重点识别：

\- 应用壳层（AppShell / Layout / MainLayout / DashboardLayout 等）
\- Sidebar / Topbar / Header 的实现方式
\- 页面内容区域的外层容器
\- 页面级 padding / margin / max-width / gap 的实际写法
\- 常见页面骨架模式（列表页、详情页、表单页、仪表盘页）
\- Card / Section / PageHeader / FilterBar / Table 等通用组件
\- 是否存在共享的 design token、theme、tailwind config、CSS variables、spacing scale、typography scale

\### 第二步：区分“事实”与“建议”
输出时请严格区分：
\- **当前代码中已经存在并被反复使用的事实性规则**
\- **当前代码中存在冲突或不一致的地方**
\- **为便于后续协作，可以提出的最小收敛建议**

注意：
\- 不要把建议伪装成既有规范
\- 不要在没有代码依据时写成“项目统一规则”
\- 如果某条规则只是局部页面特例，要明确写出来
\- 如果多个页面存在不同写法，要比较它们的覆盖范围和复用频率，再判断哪一种更接近事实标准

\### 第三步：生成一份 Markdown 规范文档
请输出一份结构完整的 Markdown 文档，文档名称建议为：

```
frontend-unified-rules.md
```

文档至少包含以下章节：

1. 文档目的与适用范围
2. 代码分析依据（你参考了哪些目录、组件、页面模式）
3. 当前项目已存在的统一规则

   \- 全局壳层 / 布局结构
   \- Sidebar / Topbar / Header 规则
   \- 页面容器与内容区规则
   \- 页面级 spacing 规则
   \- Card / Section / PageHeader 等通用区块规则
   \- 表单 / 表格 / 筛选区 / 操作区规则
   \- 字体层级 / 颜色 / 圆角 / 阴影 / token 使用规则
   \- 响应式规则

4. 常见页面骨架模板

   \- 列表页模板
   \- 详情页模板
   \- 表单页模板
   \- Dashboard 页模板

5. 当前代码中的不一致点与冲突点
6. 建议作为“统一收敛规则”的最小补充项
7. 新页面开发时必须遵守的规则清单
8. 供 AI / 开发者复用的执行原则


\### 第四步：文档写作要求
请保证文档满足以下要求：

\- 必须以“从代码中归纳出的规则”为主，不要写空泛的设计原则
\- 每一条规则尽量写清楚“证据来源”或“依据来源”
\- 对于不确定规则，要明确标注“待确认”
\- 对于明显不一致的地方，要列出冲突而不是模糊带过
\- 语言要适合团队协作使用，既能被人读懂，也能被 AI 后续引用
\- 最终产物要像一个真实项目里的规范文档，而不是泛泛而谈的教程

\### 第五步：输出格式
请按以下顺序输出：

1. 先给出你识别到的项目规则摘要
2. 再给出冲突点摘要
3. 最后输出完整 Markdown 文档正文


如果代码证据不足，请明确说明哪些结论是高置信度、哪些是中低置信度。









现在请不要再输出分析过程，也不要再输出解释说明。

请直接输出最终版 Markdown 文件内容，要求如下：

1. 文件名：`frontend-unified-rules.md`
2. 输出内容必须是完整可保存的 Markdown 正文
3. 不要附加额外聊天说明
4. 文档要适合直接保存到项目仓库中
5. 标题层级要清晰
6. 对于不确定项，统一使用“待确认”标记
7. 对于冲突项，统一放在“当前代码中的不一致点”章节
8. 文档最后增加一个章节：
   - `## AI 页面开发调用约束`
   这一章要把规则整理成适合未来直接复制给 AI 的约束清单

请开始输出完整 Markdown 文件正文。